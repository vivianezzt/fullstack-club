/* primeiro módulo do node (File system) para manipular arquivos em diretórios
const fs = require('fs');

fs.writeFileSync('hello.txt', 'Essa é a nossa primeira mensagem escrita com módulo fs!');
fs.appendFileSync('hello.txt', '\nEssa é a nossa segunda mensagem escrita com módulo fs!');
*/

const fullName = 'John Doe'
console.log(fullName)